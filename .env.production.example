# ============================================
# ConsultIA — Production Environment Variables
# ============================================
# Copy this file to .env.production and fill in the values.
# NEVER commit .env.production to version control.

# --- AWS ---
CDK_DEFAULT_ACCOUNT=386892108632
CDK_DEFAULT_REGION=eu-west-1
ENV=production

# --- Frontend (Next.js) ---
NEXT_PUBLIC_API_URL=https://ukdxe7lwqg.execute-api.eu-west-1.amazonaws.com/prod
NEXT_PUBLIC_COGNITO_USER_POOL_ID=eu-west-1_hpVoPbrFy
NEXT_PUBLIC_COGNITO_CLIENT_ID=p0ut3p0pc14k97naeav3bf82j
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51SoNWm0aaTwCcZn9placeholder

# --- Backend Secrets (stored in AWS Secrets Manager, NOT here) ---
# These are listed for documentation only — never set them as env vars.
#
# consultia/database/credentials   → Aurora PostgreSQL master password
# consultia/production/api-keys    → JSON with:
#   {
#     "ELEVENLABS_API_KEY": "...",
#     "TWILIO_ACCOUNT_SID": "...",
#     "TWILIO_AUTH_TOKEN": "...",
#     "STRIPE_SECRET_KEY": "sk_live_...",
#     "STRIPE_WEBHOOK_SECRET": "whsec_..."
#   }

# --- Domain ---
# Route 53 hosted zone for consultia.es must be created manually before deploy.
# The ACM certificate for api.consultia.es is provisioned automatically by CDK.
